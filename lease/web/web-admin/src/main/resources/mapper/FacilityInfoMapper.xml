<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.atguigu.lease.web.admin.mapper.FacilityInfoMapper">

    <!--根据公寓id查询公寓配套数据-->
    <select id="findFacilityListByApartmentId" resultType="com.atguigu.lease.model.entity.FacilityInfo">
        select id,
               type,
               name,
               icon
        from facility_info
        WHERE is_deleted=0
          AND id in (SELECT facility_id
                     FROM apartment_facility
                     WHERE apartment_id=#{id}
                       AND is_deleted=0)
    </select>
    <select id="findFacilityListByRoomId"
            resultType="com.atguigu.lease.model.entity.FacilityInfo">
        select id,
               type,
               name,
               icon
        from facility_info fi
        where is_deleted =0 and fi.id in
                (
                select rf.facility_id from room_facility rf
                 where is_deleted =0 and rf.room_id=#{id}
             )
    </select>

</mapper>
